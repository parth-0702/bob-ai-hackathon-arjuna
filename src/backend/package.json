{
  "name": "portsentinel-nexus-backend",
  "private": true,
  "version": "0.1.0",
  "description": "PortSentinel Nexus backend — simulated operational data layer for Deendayal Port (Kandla)",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "api": ".venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.3",
    "@types/node": "^22.15.3",
    "tsx": "^4.19.3",
    "typescript": "~5.8.3"
  }
}
