# No environment variables are required for the current frontend demo.
# Future frontend variables are public browser configuration, never secrets.
# Backend credentials must remain on the server and must not be committed.
